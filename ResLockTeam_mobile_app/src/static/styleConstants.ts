const headerFontSize = 30;
const subheaderFontSize = 20;
const textFontSize = 16;
const largeBorderRadius = 30;
const smallBorderRadius = 10;
const xlIconSize = 50;
const largeIconSize = 40;
const mediumIconSize = 30;
const smallIconSize = 20;
const inputMinHeight = 40;

const styleConstants = {
    headerFontSize,
    subheaderFontSize,
    textFontSize,
    smallBorderRadius,
    largeBorderRadius,
    xlIconSize,
    largeIconSize,
    mediumIconSize,
    smallIconSize,
    inputMinHeight
};

export default styleConstants;